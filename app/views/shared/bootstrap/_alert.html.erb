<div class="alert fade in alert-<%= type %>">
  <span class="close" data-dismiss="alert"><%= fa_icon 'times' %></span>
  <% if subtitle.present? %>
    <b><%= t("app.flash_message.#{subtitle}").capitalize %>!</b> <br />
  <% end %>
  <%= message %>
</div>
